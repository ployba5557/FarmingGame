// ไฟล์: QuestEnums.cs
// ใช้สำหรับกำหนดประเภทของเควสต่างๆ

public enum QuestType
{
    // เควสเก็บไอเท็ม
    GatherItems,
    // เควสส่งของ
    DeliverItems,
    // เควสพูดคุยกับ NPC
    TalkToNPC,
    // เควสปลูกผัก (เพิ่มใหม่)
    Farm,
    // เควสตกปลา (เพิ่มใหม่)
    Fish,
    //เควสเจาะหิน
    Mine

}